<h1>Add a new critter</h1>

<%= form_for( @creature , remote: true) do |f| %>
    <%= f.label :name%>
    <%= f.text_field :name %>

    <%= f.label :description %>
    <%= f.text_field :description %>

    <%= f.submit "create" %>
<% end %>